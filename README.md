# TODO:

finish determine_restaurant.py
finish determine_orders.py
setup virtual phone (maybe Google Voice?) (with gmail attached), which will return text message back to order_food.py (or get sessions working on ubereats as copyable)
finish
